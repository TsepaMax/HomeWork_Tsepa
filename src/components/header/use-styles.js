import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((ctx) => {
  return {
    header: {
      display: "flex",
      margin: "0 30px",
      padding: "0 20px",
      height: "50px",
      background: "#2d95d6",
      alignItems: "center",
      justifyContent: "space-between",
      width: "900px",
    },

    icon: {},
  };
});
